import React from 'react';
import { Icon } from 'devicon-react';
import { SkillsWrapper } from '../../styles/Wrappers';
import { RoundedRectangle } from '../../styles/Frame';

// interface SkillsProps {
//   // Properties for the Skills component
// }

const Skills: React.FC = () => {
  return (
    <SkillsWrapper>
      <RoundedRectangle>
      </RoundedRectangle>
    </SkillsWrapper>
  );
};

export default Skills;
