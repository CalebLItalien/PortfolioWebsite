import React from 'react';
import { ResumeWrapper } from '../../styles/Wrappers';

// interface ResumeProps {
// }

const Resume: React.FC = () => {
  return (
    <ResumeWrapper>
      <h1>Here we'll host a Resume</h1>
    </ResumeWrapper>
  );
};

export default Resume;
